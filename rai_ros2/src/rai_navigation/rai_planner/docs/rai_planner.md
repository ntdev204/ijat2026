# RAI Planner

`rai_planner` owns the planner interface and shared map-grid utilities. Each concrete path-planning algorithm lives in its own package, for example `rai_planner_a_star`, `rai_planner_dijkstra`, and `rai_planner_straight_line`.
